==================================================================
COPY & PASTE THIS INTO SUPABASE SQL EDITOR
==================================================================

Go to: https://supabase.com/dashboard/project/gjqbbgrfhijescaouqkx/sql/new

Then copy EVERYTHING below this line and paste it into the SQL editor.
Click "RUN" and wait for completion.

------------------------------------------------------------------

-- Enable extensions
CREATE EXTENSION IF NOT EXISTS pg_cron;
CREATE EXTENSION IF NOT EXISTS http WITH SCHEMA extensions;

-- Trigger sync immediately
SELECT extensions.http_post(
  url := 'https://gjqbbgrfhijescaouqkx.supabase.co/functions/v1/hybrid-email-accounts-v2',
  headers := '{"Content-Type": "application/json"}'::jsonb,
  body := '{}'::jsonb
);

-- Wait message
DO $$
BEGIN
  RAISE NOTICE 'Sync triggered! Wait 3-5 minutes, then check: https://www.maverickmarketingllc.com/email-accounts';
END $$;

==================================================================
DONE! After 3-5 minutes, hard refresh your browser (Cmd+Shift+R)
==================================================================
